.col-md-6.col-md-offset-3
    -if title
        %h2=title
    .list-group
        - @courses.each do |course|
            %a.list-group-item{href: "/course/#{course.id}"}
                %h4.list-group-item-heading=course.name
                %p.list-group-item-text="#{course.tutors.count} tutors"